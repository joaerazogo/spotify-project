/* describe('ToggleDialog', () => {
  it('Debe de cambiar la propiedad toggle del dialog', () => {
    const action = ToggleDialog({
      toggle: true
    });
    expect({...action}).toStrictEqual({
      type: SharedActionTypes.TOGGLE_DIALOG,
      toggle: true
    });
  })
})

 */
